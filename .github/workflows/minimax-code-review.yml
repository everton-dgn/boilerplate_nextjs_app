name: MiniMax Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write
  id-token: write
  actions: read

jobs:
  minimax-review:
    uses: everton-dgn/ci-shared/.github/workflows/minimax-code-review.yml@main
    secrets: inherit
