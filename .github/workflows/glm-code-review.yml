name: GLM Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write
  id-token: write
  actions: read

jobs:
  glm-review:
    uses: everton-dgn/ci-shared/.github/workflows/glm-code-review.yml@main
    secrets: inherit
